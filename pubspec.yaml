name: flettra_mobile
description: Flettra mobile application.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  dio: ^5.0.0
  provider: ^6.0.0
  flutter_secure_storage: ^8.0.0
  google_fonts: ^6.0.0
  intl: ^0.18.0
  socket_io_client: ^3.1.0
  shimmer: ^3.0.0
  package_info_plus: ^9.0.0
  flutter_staggered_animations: ^1.1.1
  pinput: ^6.0.1
  image_picker: ^1.2.1
  http_parser: ^4.1.2
  geolocator: ^13.0.0
  geocoding: ^3.0.0
  share_plus: ^10.0.0
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
